# How to sort the conferences

```bash
cd utils

# on first use
python -m venv .venv

source .venv/bin/activate

# on first use
python -m pip install -r requirements.txt

python process.py

rm *.yml
```
